$('#dynamic_user_update').html("<%= j render(:partial => 'projects/add_remove_users') %>")
$('#shift-modal').modal('dispose');
$(document.body).removeClass('modal-open');
$('.modal-backdrop').remove();
